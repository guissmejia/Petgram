import styled from 'styled-components'

export const Button = styled.button`
  display: flex;
  align-items: center;
  padding-top: 8px;
  background: transparent;
  border: none;
  outline: 0;
  & svg {
    margin-right: 4px;
  }
`